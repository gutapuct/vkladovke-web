import { Box, Typography } from "@mui/material";

const History = () => {
    return (
        <Box sx={{ p: 2 }}>
            <Typography variant="h4" gutterBottom>
                История действий
            </Typography>
        </Box>
    );
};

export default History;
